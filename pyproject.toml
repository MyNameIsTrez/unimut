[build-system]
requires = ["setuptools>=68", "setuptools_scm>=8"]
build-backend = "setuptools.build_meta"

[project]
name = "unimut"
dynamic = ["version"]
description = "Mutation testing for hand-marked regions of source files"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.9"
authors = [
    { name = "Sander" },
]
keywords = ["mutation testing", "testing", "c", "pycparser"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Software Development :: Testing",
]
dependencies = [
    "pycparser>=2.21",
]

[project.urls]
Homepage = "https://github.com/MyNameIsTrez/unimut"
Issues = "https://github.com/MyNameIsTrez/unimut/issues"

[project.scripts]
unimut = "unimut.unimut:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools_scm]
